/**
 * @file
 * demoTheme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.demotheme = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
